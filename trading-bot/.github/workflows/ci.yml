name: CI/CD Pipeline

on:
  push:
    branches: [ master, main ]
  pull_request:
    branches: [ master, main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pytest flake8
    
    - name: Lint with flake8
      run: |
        flake8 src --count --select=E9,F63,F7,F82 --show-source --statistics
    
    - name: Run tests
      run: |
        pytest tests/ -v || echo "No tests yet"
    
    - name: Validate imports
      run: |
        python -c "from src.strategy.rsi_momentum import RSIMomentumStrategy; print('✓ Strategy imports OK')"
        python -c "from src.strategy.risk_engine import RiskEngine; print('✓ Risk engine imports OK')"
        python -c "from src.backtest.backtest_engine import BacktestEngine; print('✓ Backtest engine imports OK')"
        python -c "from src.utils.performance import PerformanceTracker; print('✓ Performance tracker imports OK')"

  deploy-paper:
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/master' && github.event_name == 'push'
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Deploy to Paper Trading
      run: |
        echo "🚀 Deploying to paper trading environment"
        echo "This would connect to your VPS/cloud instance"
